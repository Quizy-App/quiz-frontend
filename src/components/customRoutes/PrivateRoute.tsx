import { Outlet } from "react-router-dom";

const PrivateRoute = () => {
  return (
    // userType === "candidate" ? (
    <Outlet />
    //     ) : (
    //       <Navigate to="/recruiter" />
    //     )
    //   ) : (
    //     <Navigate to="/" />
    //   );
    // }
  );
};

export default PrivateRoute;
